---
argument_specs:
  main:
    author: "Xenion1987"
    description: "Manage users and their SSH public key enrollment via Ansible on Linux systems."
    # Variable documentation is maintained in defaults/main.yml via @var
    # annotations and rendered into README.md by ansible-doctor. The keys of
    # `user_management_users` are documented in the table in HEADER.md, since
    # @var cannot express nested keys.
    #
    # Populate options{} again only when runtime argument validation
    # (type checks, required fields, choices) is wanted back.
    options: {}
